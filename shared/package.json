{
  "name": "@ai-chat/shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "rimraf": "^3.0.0",
    "typescript": "^5.2.0"
  },
  "dependencies": {
    "zod": "^3.22.0"
  }
}
