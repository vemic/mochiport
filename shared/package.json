{
  "name": "@ai-chat/shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "type-check": "tsc --noEmit",
    "test": "jest",
    "test:watch": "jest --watch"
  },  "devDependencies": {
    "@types/jest": "^29.5.4",
    "jest": "^29.6.4",
    "rimraf": "^3.0.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.0"
  },
  "dependencies": {
    "zod": "^3.22.0"
  }
}
